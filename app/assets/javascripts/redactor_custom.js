  $(document).ready(
    function()
      {

        $('#teacher_content').redactor({
          minHeight: 300,
        });
        $('#case_content').redactor({
          minHeight: 300,
        });
        $('#project_background').redactor({
          minHeight: 300,
        });
        $('#project_question').redactor({
          minHeight: 300,
        });
        $('#project_feature').redactor({
          minHeight: 300,
        });
        $('#group_specific_time').redactor({
          minHeight: 300,
        })
      }
    );
