<%= form_for [:admin, @notice],:url => { :controller => '/admin/notices', :action => 'update', :id => @notice },:html => { :multipart => true } do |f| %>
  <%= f.kindeditor :content%>
  <%= f.submit "更新", class: "btn btn-primary"%>
<% end %>
